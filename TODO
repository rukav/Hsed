- Error handling
- What is the scope of Last RE: one Sed command or all commands
- Parse BRE
- Documentation
- Formatting
- Share state between parser invocation for the sed commands
- Check the parser complience to the standard
- Use Bytestring instead of String
- Applicative parsing 
- Consider Iteratee
- Fix escape '\\' '\n' in textFun Parsec.hs
- Character classes testing
- Perf tuning (criterition, etc)
- Substitute flags parsing error to handle proper order of flags
- Organize imports (import only needed functions)
- AppendPat and EOF of input processing. Need to be the multiple files EOF not only one
- Consider using QuickCheck to generate test cases per command
- State and data-accessors
- Parsec unesc refactoring regarding "\\n"
- Empty lhs proper error handling in the sed parser (s///). First RE should not be empty
- s1\1121 is broken tests2/rhs-escapes.sed
- pat = a*, str = "an", repl "x"  => result "xxnx" instead "xnx" 
- haddock, cabal